var anumber = 10;

let myregex = /foo/;
const myValues : Array<Number> = [10,2,3,NaN];

const person : {
    firstname: string;
    lastname: string;
    cool?: boolean;
} = {
    firstname: "jack",
    lastname: "harrington",
    cool : true,
}

const ids : Record<number, string> = {
    10: 'a',
    20: 'b',
}

ids[30]='c'